<% @results.each do |result| %>
  <table class="table">
    <tr>
      <td>
        <%= result.name %>
      </td>
      <td>
        <%= link_to "Anzeigen",  "recipes/" + result.id.to_s %>
      </td>
    </tr>
  </table>
<% end %>
<% unless @results.length > 0 || @results==nil %>
  Keine Ergebnisse
<% end %>
